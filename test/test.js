const army = require('../commands/army');


test('test suite is working', () => {
    expect(true).toBe(true);
});


// ---------------------------------------------------------
// here's an example of a typical unit test format.
// it follows the Arrange,Act,Assert unit test process.
// for instance to test a multiply method that takes
// 2 numbers and returns their product, you might work like this:
// Arrange: let a = 2; let b = 9;
// Act: let result = multiply(a,b);
// Assert: expect(result).toBe(18);
// running the tests from the command line can be done thusly:
// npm run test
// ---------------------------------------------------------
test("example test", () => {
    // ---------------------------------------------------------
    // Arrange
    // use this section to create test arguments and other
    // fixtures that will be used by the test. As well as
    // setting the uut variable (unit under test).
    // ---------------------------------------------------------
    // todo

    // ---------------------------------------------------------
    // Act
    // here you will use the arguments setup in
    // the Arrange section and pass them in
    // to the unit under test (uut).
    // ---------------------------------------------------------
    // todo

    // ---------------------------------------------------------
    // Assert
    // after you _act_ on your unit under test, likely by simply
    // calling the uut(arrangedArgs), you should assert
    // that the result of such a call is as expected
    // ---------------------------------------------------------
    // todo
})